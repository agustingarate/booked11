// import type { CreditModel } from "../../data/models/CreditModel";
// import type { CreditType } from "../../data/models/Types";
// import type { CreditsRepository } from "../repository/CreditsRepository";

// class GetBalanceUseCase {
//   private creditsRepository: CreditsRepository;

//   constructor(creditsRepository: CreditsRepository) {
//     this.creditsRepository = creditsRepository;
//   }

//   async execute(creditType: CreditType): Promise<CreditModel> {
//     return await this.creditsRepository.getBalance(creditType);
//   }
// }

// export { GetBalanceUseCase };
